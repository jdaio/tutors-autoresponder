/**
 * -----------------------------------------------------------------------------
 * Tutors.com / Gmail Autoresponder Settings
 * -----------------------------------------------------------------------------
 */

module.exports = {
    /**
     * Credential Settings
     */
    credentialDir: './credentials', // Omit trailing slashes.
    credentialGoogle: 'client_secret.json', // Google account secret filename.
    credentialSmtp: 'smtp_credentials.json', // SMTP credentials.

    /**
     * Admin Information
     */
    adminEmail: 'admin@example.com', // Email the server mail is sent to.
};
